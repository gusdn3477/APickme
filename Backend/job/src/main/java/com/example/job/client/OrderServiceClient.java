package com.example.job.client;
// 받아야할 서비스 정보 입력 (회사번호)
/*
package com.example.jobservice.client;

import com.example.jobservice.error.FeignErrorDecoder;
import com.example.jobservice.vo.ResponseOrder;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import java.util.List;

// order-service -> http://127.0.0.1:50001/{userId}/orders
@FeignClient(name = "order-service", configuration = FeignErrorDecoder.class)
public interface OrderServiceClient {

    @GetMapping("/{userId}/orders")
    List<ResponseOrder> getOrders(@PathVariable String userId);

    @GetMapping("/{userId}/orders_wrong")
    List<ResponseOrder> getOrdersWrong(@PathVariable String userId);
}
*/
/*
@FeignClient(name="인사담당자서비스")
public interface "인사담"Client{
    @GetMapping("/hr/{empNo}")
    ResponseHr getHr(@PathVariable String empNo);
    ResponseHr getHr(@PathVariable String 추가로받아올거);

}
 */
